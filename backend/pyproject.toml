[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]  # pyflakes + pycodestyle + import sorting + pyupgrade

[tool.pytest.ini_options]
testpaths = ["tests"]
